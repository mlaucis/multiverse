import keyMirror from 'keyMirror'

let consts = keyMirror({
  ANALYTICS_METRICS_REQUEST: null,
  ANALYTICS_METRICS_SUCCESS: null,
  ANALYTICS_METRICS_FAILURE: null,
})

consts['TIME_FORMAT_RANGE'] = 'YYYY-MM-DD'

export default consts